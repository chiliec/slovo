.superpowers/
.gradle/
build/
*.iml
.idea/
local.properties
xcuserdata/
.DS_Store
content-prep/node_modules/
content-prep/.audio-cache/
.design-handoff/
*design-handoff*.zip
# Release signing — never commit the keystore or its passwords
keystore.properties
*.jks
*.keystore

# Built distributable APKs
dist/
*.apk

# iOS release tooling — resolved fresh by CI under Ruby 3.3 (a lock resolved
# under local Ruby 2.6 pins gems/bundler that CI cannot install).
Gemfile.lock
vendor/
fastlane/.env
fastlane/report.xml
fastlane/screenshots/
fastlane/test_output/
fastlane/play-metadata/
build/ios/

# Google Play service account key — never commit
play-service-account.json

# App Store Connect API key and signing material — never commit
AuthKey_*.p8
*.p12
*.mobileprovision
*.cer

# Personal contact detail required by deliver but kept local
**/phone_number.txt
scripts/audio-review.json
