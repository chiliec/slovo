CREATE TABLE user_stats (
    id              INTEGER NOT NULL PRIMARY KEY,
    xp              INTEGER NOT NULL DEFAULT 0,
    streak_days     INTEGER NOT NULL DEFAULT 0,
    last_active_day INTEGER NOT NULL DEFAULT 0
);

select:
SELECT xp, streak_days, last_active_day FROM user_stats WHERE id = 0;

initRow:
INSERT OR IGNORE INTO user_stats(id, xp, streak_days, last_active_day) VALUES (0, 0, 0, 0);

update:
UPDATE user_stats SET xp = :xp, streak_days = :streak, last_active_day = :day WHERE id = 0;
